name: conduit_app_test
description: An empty conduit application.
version: 0.0.1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  conduit: ^3.0.11
  build_runner:
  build_web_compilers:

dev_dependencies:
  test: ^1.16.5
  conduit_test: ^3.0.11
